name: Auto CI/CD

on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

concurrency:
  group: poma-auto-cicd-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  dev-deploy:
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository }}
    uses: ./.github/workflows/deploy-gcp-vm.yml
    with:
      deploy_environment: dev
      terraform_action: plan
      deploy_app: false
      tailscale_enabled: false
      trading_mode: paper
      data_provider: fixture
      allow_live_trading: false
      deploy_smoke: false
    secrets: inherit

  stg-deploy:
    if: ${{ github.event_name == 'push' }}
    uses: ./.github/workflows/deploy-gcp-vm.yml
    with:
      deploy_environment: stg
      terraform_action: apply
      deploy_app: true
      # Tailscale is operator-convenience only: all automation uses IAP SSH, and VNC/SSH access
      # is available on demand via `gcloud compute ssh --tunnel-through-iap`. Skipping it removes
      # a slow, failure-prone step from every merge. Flip back on via manual workflow_dispatch
      # (or for live trading) when tailnet access is actually wanted.
      tailscale_enabled: false
      trading_mode: paper
      data_provider: yahoo
      allow_live_trading: false
    secrets: inherit

  stg-configure-gateway:
    needs: stg-deploy
    uses: ./.github/workflows/ib-gateway-ops.yml
    with:
      deploy_environment: stg
      action: configure-paper
      log_lines: 200
    secrets: inherit
