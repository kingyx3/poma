# Gateway helper refresh

Use this workflow when an existing VM has stale Gateway setup.

1. Run **Refresh Gateway Helper** for the same environment.
2. Rerun **IB Gateway Ops**.

The refresh workflow reinstalls the helper and repairs the service on the VM.
